# =============================================================================
# Environment Configuration
# =============================================================================
# LOCAL DEVELOPMENT: Copy this file to .env and fill in your values
# PRODUCTION/DEPLOYMENT: These values are set via GitHub Secrets in CI/CD
# =============================================================================

# Azure OpenAI / Azure AI Foundry Configuration
# For Azure OpenAI: https://<resource>.openai.azure.com/
# For Azure AI Foundry: https://<resource>.services.ai.azure.com/models/<deployment>/chat/completions?api-version=xxx
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# API Key for Azure OpenAI or Azure AI Foundry
# GitHub Secret: AZURE_OPENAI_API_KEY
AZURE_OPENAI_API_KEY=your-api-key-here

# Main deployment for complex tasks (Carlos, Ronei, Terraform, Auditor, Recommender)
# Recommended: gpt-4o or gpt-4-turbo
# GitHub Secret: AZURE_OPENAI_DEPLOYMENT_NAME
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

# Mini deployment for simple analysis tasks (Security, Cost, Reliability, Requirements)
# Recommended: gpt-4o-mini (10x cheaper than gpt-4o, 50-70% overall cost savings)
# If not set, falls back to gpt-4o-mini as default
# GitHub Secret: AZURE_OPENAI_MINI_DEPLOYMENT_NAME
AZURE_OPENAI_MINI_DEPLOYMENT_NAME=gpt-4o-mini

# API Version (only for traditional Azure OpenAI, not Azure AI Foundry)
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# JWT Secret for authentication
# GitHub Secret: JWT_SECRET_KEY
JWT_SECRET_KEY=your-secret-key-here

# =============================================================================
# Notes
# =============================================================================
# - The system automatically detects Azure AI Foundry endpoints by checking
#   for 'services.ai.azure.com' in the AZURE_OPENAI_ENDPOINT URL
# - For production deployments, configure these as GitHub Secrets in your repo
# - Never commit the actual .env file with real credentials to version control
